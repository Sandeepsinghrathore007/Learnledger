# AI assistant mode:
# direct    = browser calls OpenRouter directly. No Firebase billing, but the AI key is exposed to the browser.
# functions = Firebase Cloud Functions backend using OpenRouter. Requires Blaze plan.
VITE_AI_ASSISTANT_MODE=direct
#
# Frontend direct AI setup:
# Use browser keys only for private/local testing.
# Do not ship these values to GitHub Pages or any public static deployment.
# VITE_OPENROUTER_API_KEY=your_openrouter_key
# Optional escape hatch for public builds. Keep disabled unless you explicitly accept exposed keys.
# VITE_AI_ASSISTANT_ALLOW_UNSAFE_DIRECT=true
# Optional model overrides:
# VITE_OPENROUTER_MODEL=openai/gpt-4o-mini
#
# Firebase Functions backend setup:
# firebase functions:secrets:set OPENROUTER_API_KEY
# VITE_FIREBASE_FUNCTIONS_REGION=us-central1

# Firebase Web SDK config
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id
